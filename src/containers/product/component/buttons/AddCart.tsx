import { Box, styled } from '@mui/material';

const AddCartButtonWrapper = styled(Box)(({ theme }) => ({}));

const AddCartButton = () => {
  return <AddCartButtonWrapper>AddCartButton</AddCartButtonWrapper>;
};

export default AddCartButton;

import { Box, styled } from '@mui/material';

const SearchProductWrapper = styled(Box)(({ theme }) => ({}));

const SearchProductInput = () => {
  return <SearchProductWrapper>Search input</SearchProductWrapper>;
};

export default SearchProductInput;
